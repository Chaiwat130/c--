#include<stdio.h>

int linearSearch(int arr[], int size, int target) {
    for (int i = 0; i < size; i++) {
        if (arr[i] == target) {
           return i; // ส่งส่ คืนคื ดัชดั นีหนี ากพบ
        }
    }
    return -1; // ส่งส่ คืนคื -1 หากไม่พบม่
}
